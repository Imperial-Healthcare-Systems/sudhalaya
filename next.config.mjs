/** @type {import('next').NextConfig} */
const nextConfig = {
  // The legacy storefront engine is served verbatim from /public/sudhalaya.js
  // and bootstrapped via next/script. Nothing to transform here.
  reactStrictMode: true,
};

export default nextConfig;
